# Objective Warm-start 收尾强化：修复反馈

## 最终验收问题

1. **strong reference 比旧60-eval reference平均高多少？** 0.012038。
2. **原来的负 Gap 还有多少？** 旧口径 248 条；strong reference 后 0 条，占 0.0%。它仍是 bounded reference。
3. **Episodic 提议区域的 containment rate 是多少？** Teacher=200 时 25.0%。
4. **Episodic 相比 No Memory 把区域体积缩小多少？** 平均从 0.062193 降到 0.010873，缩小 82.5%。
5. **Procedural region 本身是否有价值？** budget=10 时相对 Pure 的配对 Objective 差值为 0.0085；95% CI 不跨0。LLM−Procedural deterministic 为负，说明 L2 区域有小幅价值，当前主要问题出在 LLM 使用方式。
6. **Episodic deterministic search 与 Episodic+LLM 谁更好？** budget=10 时 LLM−deterministic 为 0.0051。
7. **LLM 增益来自 Memory 信息还是 LLM 推理？** 确定性 Episodic−Pure 衡量 Memory 区域本身，值为 0.1140；LLM−确定性值为 0.0051，后者95% CI 跨0。budget=10 的增益主要来自 Memory 信息。
8. **Episodic Warm-start 在 Objective evaluations 上节省多少？** 固定 budget=10 时 Pure 与 Warm-start 平均都使用 10 次，直接节省为0。budget=20 的 strong-reference 95% gain 到达率为0%与27.8%；Pure 无成功样本，无法给出公平的平均 evaluation 节省量。
9. **真实 wall-clock 是否也节省？** budget=10 的 Pure / Episodic warm 在线均值为 1269.4 / 2631.1 ms；每次差额 -1361.6 ms，结论为 no break-even。
10. **200条 Teacher 的离线成本是多少？** 11000 次 Objective evaluations、639.1 秒、0次 LLM。
11. **多少 query 能摊平 Teacher 成本？** N/A；状态为 no break-even。
12. **归一化以后 dp_tolerance 是否仍最敏感？** 是；排序为 dp_tolerance > max_speed_mps > dist_threshold。
13. **本轮是否修改 Objective？** 没有。hash 与冻结基线一致。
14. **本轮是否修改 v2 历史实验？** 没有。目录清单 hash 与运行前一致。
15. **完整测试结果是多少？** 329 passed in 504.61s (0:08:24)。
16. **当前限制？** holdout 仅8辆适用；reference 仍有预算上限；只有一个 provider/model 的既有输出；敏感性是局部 trace 统计；固定预算不直接证明 evaluation 或 wall-clock 节省。

## 完整性检查

- strong reference：8 辆，0次新 LLM
- region quality：576 行
- deterministic ablation：1440 / 1440 行
- paired keys 全部对齐：True
- Objective 与 v2/旧实验：均保持冻结
